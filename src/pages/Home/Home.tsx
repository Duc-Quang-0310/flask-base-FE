import { Container } from "@mui/material";
import React from "react";

const Home = () => {
  return (
    <Container sx={{ height: "auto" }} maxWidth={false}>
      Hello
    </Container>
  );
};

export default Home;
